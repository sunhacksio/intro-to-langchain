[project]
name = "intro-to-langchain"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "langchain",
    "langchain-core",
    "langchain-openai",
    "langserve[all]",
    "python-dotenv",
]

[dependency-groups]
dev = [
    "langchain-cli>=0.0.37",
    "ruff>=0.13.2",
]
